.env
.env.*
__pycache__/
*.pyc
data/*.db
data/photos/
# Downloaded menu images, re-fetchable from the URLs in crawl_hits.json.
data/menu_images/
node_modules/
.wrangler/
.DS_Store
data/raw/
data/venues.csv
logs/
data/*.bak
scratch/

# The crawler's page-text cache (ingest/crawl_sites.py -> ingest/read_pages_llm.py).
# Rebuildable from a --recrawl, one file per happy-hour page, thousands at
# full-corpus scale. The ANSWERS it produces are committed: data/deals_pages_llm.json.
data/pages/
scratchpad/google_eyes/
data/agent_reads/
scratchpad/turtle/
scratchpad/*_raw.html

# The events reader's saved pages, sibling of data/agent_reads/. The
# grounded ANSWERS are committed: data/events_reads.json.
data/events_reads/
